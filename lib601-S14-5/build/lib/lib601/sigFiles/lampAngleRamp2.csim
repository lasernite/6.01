#CMax simulation
import lib601.sig as sig
import math

lampAngleSignal = sig.ListSignal([float(i-50)/200. for i in range(100)])
lampDistanceSignal = sig.ConstantSignal(0.667)
nSamples = 100
deltaT = 0.02
